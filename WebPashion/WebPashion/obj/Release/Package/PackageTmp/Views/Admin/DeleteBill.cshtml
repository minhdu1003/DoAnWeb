@model WebPashion.Models.BILL

@{
    ViewBag.Title = "DeleteBill";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header card-header-primary">
                        <h4 class="card-title ">Quản lý chi đơn hàng</h4>

                    </div>
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 style="display:inline-flex;margin-left:450px;margin-top:50px" class="modal-title" id="exampleModalLongTitle">Bạn có chắc muốn xóa đơn hàng&#160;<p style="font-weight:bold">@Model.ID</p>&#160;không !</h5>
                        </div>
                        @*  *@
                        <div>


                            @using (Html.BeginForm())
                            {
                                @Html.AntiForgeryToken()

                                <div style="margin:20px 0px 20px 550px" class="form-actions no-color">
                                    <input type="submit" value="Xóa" class="btn btn-primary" />
                                    <a style="margin-left:50px;" href="@Url.Action("GetBill","Admin")">

                                        <button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg">Trở về</button>

                                    </a>
                                </div>
                            }
                        </div>
                        @*  *@
                        <div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
