@using WebPashion.Models
@model List<WebPashion.Models.PRODUCT_TYPE>
@{
    ViewBag.Title = "GetSize";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header card-header-primary">
                        <h4 class="card-title ">Quản lý loại sản phẩm</h4>

                    </div>
                    <div class="card-body">
                        <a href="@Url.Action("AddType","Admin")">

                            <button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg">+ Thêm mới</button>

                        </a>
                        <div class="table-responsive">
                            <table class="table">
                                <thead style="text-align:center" class=" text-primary">
                                <th>
                                    Mã loại
                                </th>
                                <th>
                                    Loại
                                </th>
                                <th>
                                </th>
                                <th>
                                </th>
                                </thead>
                                <tbody>
                                    @for (int i = 0; i < Model.Count(); i++)
                                    {
                                        <tr style="text-align:center;">

                                            <td>
                                                @Model[i].Type

                                            </td>
                                            <td>
                                                @Model[i].TypeName
                                            </td>
                                            <td>
                                                <a href="@Url.Action("EditType","Admin", new { id = Model[i].Type })"> <img style="width:20px" src="~/Content/images/IMG/pencil.png" /></a>
                                            </td>
                                            <td>
                                                <a href="@Url.Action("DeleteType","Admin", new { id = Model[i].Type })"><img style="width:20px" src="~/Content/images/IMG/cancel.png" /></a>
                                            </td>
                                        </tr>

                                    }

                                </tbody>
                                <tr>
                     

                                </tr>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



